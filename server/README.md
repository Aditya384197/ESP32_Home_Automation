# Smart Home V2 backend

Reference backend for the Internet Edition. It is designed for Cloudflare Workers + D1 and intentionally keeps the ESP32 on an outbound HTTPS connection; no inbound port forwarding is required.

See the repository README for setup. Keep `ADMIN_PASSWORD`, device tokens and session credentials private.
